using System;

namespace TesteOrientacoesObjeto
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // Escreva uma classe que representa um veículo conforme os requisitos abaixo:

            // 1 A classe deve possuir uma propriedade pública do tipo inteiro para
            //    armazenar a valocidade atual:
            // 2 A classe deve possuir um atributo privado do tipo booleano para
            //    armazenar se o veículo está ligado;
            // 3 A classe deve possuir um método privado para frear o carro
            //    (zerar a velocidade atual);
            // 4 A classe deve possuir um método público para ligar o carro
            //    (atribuir verdadeiro ao atributo do item 2);
            // 5 A classe deve possuir um método público para desligar o carro
            //    (atribuir falso ao atributo do item 2. Se o veículo estiver com 
            //    uma velocidade atual superior a zero, deve primeiramente frear o mesmo);
            // 6 A classe deve possuir um método público que retorne se o veículo está
            //    ligado (atributo do item 2);
            // 7 A classe deve possuir um método público para acelerar o veículo
            //     (o método deve receber por parâmetro a nova velocidade e somente
            //     atribuir ela como velocidade atual se o veículo estiver ligado
            //     a velocidade for menor ou igual a 100).




















        }
    }
}
